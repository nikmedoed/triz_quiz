{% extends 'base.jinja2' %}

{% block content %}
  <h1>{{ step.title }}</h1>
  <p class="description">{{ step.text }}</p>
  {% if phase > 0 %}
    {% if ideas %}
      {% if phase == 1 %}
      <p class="question">Голосование за идеи</p>
      {% endif %}
      <div class="ideas-grid">
        {% for idea in ideas %}
        {% set voters = voters_map.get(idea.id) or [] %}
        <div class="idea-card">
          <div class="idea-header">
            <img class="avatar small" src="/avatars/{{ idea.author.id }}.png" title="{{ idea.author.name }}" />
            <span class="author-name">{{ idea.author.name }}</span>
            <div class="idea-vote-count">★ {{ voters|length }}</div>
          </div>
          <div class="idea-text">{{ idea.text }}</div>
          <div class="idea-meta">ответ занял {{ idea.delay_text }}</div>
          <div class="idea-voters">
            {% for v in voters %}
              <img src="/avatars/{{ v.id }}.png" title="{{ v.name }}" />
            {% endfor %}
          </div>
        </div>
        {% endfor %}
      </div>
    {% else %}
      <p class="question">Ответов нет</p>
    {% endif %}
  {% endif %}
{% endblock %}
